/**
 * Controller for user rating page
 */
(function () {
    "use strict";

    angular
        .module('app')
        .controller('UserRating', UserRating);

    UserRating.$inject = ['$state', '$ionicHistory'];

    function UserRating($state, $ionicHistory) {

        var vm = this;

    }
})();
