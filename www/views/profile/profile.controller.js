(function () {
    "use strict";

    angular
        .module('app')
        .controller('Profile', Profile);

    Profile.$inject = ['$rootScope', '$state'];

    function Profile($rootScope, $state) {

        var vm = this;

    }
})();
